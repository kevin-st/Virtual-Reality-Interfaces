<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="CameraStream.CameraStream" android:versionCode="1" android:versionName="1.0">
  <uses-sdk android:minSdkVersion="16" />
  <application android:label="CameraStream"></application>
  <uses-permission android:name="android.permission.CAMERA" /> <!-- The application should have permissions to use the camera. -->
  <uses-feature android:name="android.hardware.camera" android:required="false" /> <!-- We must also declare the use of camera features. This causes the app only to be installed on a device which has a camera of has the features on it.-->
</manifest>